import type { NextPage } from "next";
import Landing from "../components/modules/Landing";

const Home: NextPage = () => {
  return (
  <div className="container">
    <Landing />
  </div>
  );
};

export default Home;
